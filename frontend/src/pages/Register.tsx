import { Link } from "react-router-dom";

export default function Register() {
  return (
    <div>
      <h1>Register Page</h1>
      <nav>
        <Link to="/">Login</Link>
      </nav>
    </div>
  );
}